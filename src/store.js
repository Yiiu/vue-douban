import vue from "vue";
import vuex from "vuex";

vue.use(vuex);

const state = {
    // 放置初始状态
}
const mutations =  {
    // 放置状态变更函数
}
export default new Vuex.Store({
    state,mutations
})